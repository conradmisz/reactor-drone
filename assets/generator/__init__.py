"""Instructor-only Class-090 procedural entity model and texture atlas generator.

Gen-1 establishes the renderer foundation and proves the end-to-end pipeline with a
single reference entity. This package is NOT part of the C++ engine/game build and is
never wired into CMake/CTest. Students never run it; they consume the committed atlas
PNGs and sidecar JSON as plain textures.

See `.kiro/specs/090-11-generator-spec/` for the requirements, design, and tasks.
"""
